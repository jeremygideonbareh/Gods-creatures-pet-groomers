table:
  name: bookings
  schema: public
object_relationships:
  - name: pet
    using:
      foreign_key_constraint_on: pet_id
select_permissions:
  - role: user
    permission:
      columns:
        - addons
        - advance_paid
        - created_at
        - customer_name
        - email
        - id
        - notes
        - pet_id
        - phone
        - preferred_date
        - preferred_time
        - service
        - status
        - total_price
        - transaction_id
        - user_id
      filter:
        user_id:
          _eq: X-Hasura-User-Id
      allow_aggregations: true

