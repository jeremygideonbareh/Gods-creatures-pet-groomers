table:
  name: providers
  schema: auth
array_relationships:
  - name: user_providers
    using:
      foreign_key_constraint_on:
        column: provider_id
        table:
          name: user_providers
          schema: auth
