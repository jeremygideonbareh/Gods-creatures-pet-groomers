/**
 * Store catalog moved to the CMS `store_catalog` jsonb section (2026-08-01).
 * Only STORE_PHONE remains here — products + categories are now admin-editable
 * via the ContentEditor "Store Catalog" tab (see `storeCatalog` in site-content.ts).
 */

export const STORE_PHONE = "8798897732";
